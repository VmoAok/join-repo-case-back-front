import styles from '../styles/Banner.module.css';
import Categories from './Categories'; // importe o componente

const Banner = () => {
  return (
    <div className={styles.banner}>
    </div>
  );
};

export default Banner;